conference:
  name: CaptionMesh Demo

gemini:
  # Gemini Live Transcribe sessions have a documented ~10 minute limit.
  # Rotate a full minute before that, not at the exact boundary.
  session_rotation_seconds: 540

stages:
  - id: main
    name: Main Stage
    language: auto
    targets:
      - es
    source:
      type: file
      path: data/audio/nerdearla-demo-60s.wav
